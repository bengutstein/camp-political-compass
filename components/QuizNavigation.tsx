export function QuizNavigation({ onBack, onNext, isFirst, isLast, disabled, loading }: { onBack: () => void; onNext: () => void; isFirst: boolean; isLast: boolean; disabled: boolean; loading: boolean }) {
  return <div className="fixed inset-x-0 bottom-0 z-30 flex items-center justify-between gap-5 border-t rule bg-paper/95 px-5 py-3 pb-[calc(env(safe-area-inset-bottom)+0.75rem)] backdrop-blur sm:static sm:mt-8 sm:border-0 sm:bg-transparent sm:px-0 sm:py-0 sm:backdrop-blur-none"><button type="button" onClick={onBack} disabled={isFirst || loading} className="min-h-11 px-2 py-3 text-sm font-bold underline underline-offset-4 disabled:cursor-not-allowed disabled:opacity-30">← Previous</button><button type="button" onClick={onNext} disabled={disabled || loading} className="min-h-12 bg-moss px-6 py-4 text-sm font-bold text-white transition hover:bg-ink disabled:cursor-not-allowed disabled:opacity-40">{loading ? "Calculating…" : isLast ? "See My Results" : "Continue →"}</button></div>;
}
